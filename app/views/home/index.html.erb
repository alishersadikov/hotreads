<h2>Recently created links</h2>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>URL</th>
    </tr>
  </thead>

  <tbody>
    <% @links.each do |link| %>
      <tr>
        <td><%= link[:title] %></td>
        <td><%= link[:url] %></td>
      </tr>
    <% end %>
  </tbody>
</table>
